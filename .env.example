# Anthropic API key for Claude
ANTHROPIC_API_KEY=sk-ant-...

# Telegram bot token — use separate bots for test vs prod
# Create via @BotFather on Telegram
TELEGRAM_BOT_TOKEN=123456:ABC-DEF...

# Gateway auth token (shared secret between flyapp.so and flyclawd)
# Must match OPENCLAW_TOKEN on the flyapp.so side
GATEWAY_TOKEN=your-secret-token-here
